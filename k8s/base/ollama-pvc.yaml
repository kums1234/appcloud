apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: appcloud
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # storageClassName is set per overlay (standard for minikube, gp3 for EKS, managed-csi for AKS, etc.)