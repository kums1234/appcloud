apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: appcloud
  labels:
    app: api
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
